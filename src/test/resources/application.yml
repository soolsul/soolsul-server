spring:
  datasource:
    url: jdbc:mysql://database-1.cbg3kx2zb4pv.ap-northeast-2.rds.amazonaws.com:3306/soolsul-dev
    username: admin
    password: soolsul661899!
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  main:
    allow-bean-definition-overriding: true

aws:
  s3:
    images:
      bucket:
        name: soolsul-images
      region:
        name: ap-northeast-2
      endpoint:
        url: https://soolsul-images.${aws.s3.images.region.name}.amazonaws.com/
    credentials:
      access:
        key: access-key
      secret:
        key: password
